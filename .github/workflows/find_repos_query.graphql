query($org: String!) {
  organization(login: $org) {
    repositories(first: 100) {
      nodes {
        name
        repositoryTopics(first: 10) {
          nodes {
            topic {
              name
            }
          }
        }
      }
    }
  }
}
